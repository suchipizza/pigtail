# Run log
One entry per session: date · session id · tasks · outcomes · cost · next.

## 2026-09-25 · session 1 (interactive orchestrator)
- Tasks: M0-T1..T5 done; M2-T1, M2-T2 started with research subagents.
- Outcomes: scaffold (uv/py3.12, ruff, mypy strict, pytest: 36 passed, 2 smoke tests skipped by default); `LLMClient` (F15) with both backends. **Subscription smoke test passed** (structured output, `claude -p --json-schema`). Billing check: `claude auth status` reports authMethod `claude.ai`, subscription plan, `ANTHROPIC_API_KEY` not set → usage is on the subscription, not API billing (M0 acceptance). The api smoke test is skipped: no key. docker compose: Postgres + SeaweedFS healthy, S3 round-trip and bad-credential rejection verified. MinIO image unavailable → ADR-005.
- Cost: LLM ≈ 2 smoke calls on subscription (no API spend). No BigQuery or hosting spend.
- Next: push + CI green (M0-T6); M1-T1..T3 capture core; merge M2 research outputs; verifier spot-check.
- 2026-09-25 incident: commit "docs(research): source matrix…" was pushed after the private-data scan flagged 4 findings, because of a shell chaining mistake. The findings were one platform's public role address (hello@…), not personal data. Fix: scan allowlists role addresses (ADR-011); commits are gated on a passing scan.
- 2026-09-25 verifier M0 @55980a2: PASS. CI run 36138521818 green; compose db+S3 healthy and S3 round trip verified; ops files and H1 present; subscription structured-output smoke re-run and passed (api skipped: no key); auth = claude.ai subscription, no ANTHROPIC_API_KEY, key-stripping verified live; private-data scan and history clean; tests honest. Required fix (done same day): over-broad phone redaction turned dates/counts into [phone]; now only +international or labelled numbers. Also applied: prompt fingerprint in cache key + provenance, cache served while paused, session count includes failed calls, more auth env vars stripped, runner scans before push and narrower limit detection.
- 2026-09-25 verifier M2-T4 (citation spot-check, 17 literature + 29 matrix/memo citations): FAIL. No fabrication: 31 verified, 7 minor issues, 1 wrong (GH Archive #137 described as unanswered; it was answered and closed), 7 unreachable directly but confirmed via search. Failures: one wrong claim, several uncited claims, §8.1 sources not audited (GitHub dependency graph, Discord/Slack, careers pages, press). Corrections delegated to researcher; re-check of changed lines to follow. ADR-009 context updated with the verifier's findings.
- 2026-09-25 verifier M2-T4 round 2 @191c44d: PASS. All 12 corrections verified; every new citation (TM-26..31) resolved and matched; matrix covers all R2.2 and §8.1 sources (not audited: OpenDigger mirror, hosted job boards). Optional: Algolia wording, yc-oss citation, and #312 notes (GitHub Support confirmed the Oct 2025 drop was an Events API cache issue; a community report says merged PRs no longer emit PullRequestEvent).
- 2026-09-25 verifier M3 round 1 @e4988ec: FAIL (docs only). Outcome model, thresholds JSON, H2 and private-data scan PASS; 20+ legal citations verified. Fixes: 'implemented' controls not yet on main relabelled 'I (M1, pending merge)'; CB-07 status updated; ADR-022 made the single list of preconditions for person-level sources. Recommendations applied. Round-2 re-check after the M1 merge.
- 2026-09-25 M1 core (engineer): schemas v0 + examples, migrations 0001/0002 with a forward-only runner, content-addressed snapshot store (local and S3), connector base (terms, enable flag, rate limit, retries, UA, pseudonymization at ingest, snapshot before parse), run records, replay hook, GH Archive connector, bot filter v0, velocity detection, CB-04 raw purge. 143 tests pass (DB/S3 tests against compose). Real smoke run: 17 hours at 1.3–2.3 s/hour and ≤ 186 MB; star coverage ≈ 0.007 against public counts (25 repos, 37 min). All real dumps purged; no cases opened. → ADR-027, ADR-028.
- 2026-09-25 verifier M3 round 2 @4f55e73: FAIL (docs only). All round-1 fixes confirmed; every 'I' claim matched code on main; 143 tests pass; scan clean. Remaining: CB-04 described inconsistently (now marked partly implemented), stale 'pending merge' conditions in the LIA, outdated commit in the spec header. Recommendations applied: campaign-rule numbers and baseline_days added to the thresholds JSON.
- 2026-09-25 verifier M3 round 3 @7369673: FAIL (docs only). Round-2 fixes confirmed. Remaining: CB-11 shown as planned but implemented in codebook §12; LQ-25 still called CB-04 'proposed'. Fixed the same day.
- 2026-09-25 verifier M3 round 4 @fc0ee64: PASS. Round-3 fixes confirmed; full CB-01..CB-21 sweep: no contradictions with code on main (implemented: CB-07, CB-11; partly: CB-04; the rest planned). Private-data scan clean (110 files). **M3 accepted**: outcome model, compliance pack, H2 raised. Note: M4/M5 coding prompts must carry the codebook P1 instruction to ignore personal characteristics.
- 2026-09-25 privacy controls (engineer): CB-01, 05, 08, 13 done; CB-03, 06, 18 partly done (ADR-030). Migration 0003 (deletion_log append-only, optout, request log). 206 tests pass (Postgres + S3 required); scan clean.
- 2026-09-25 HN capture (engineer): Firebase + Algolia connectors (off by default, ADR-022 hold), rank poller (live check: 500 ids, 30 stories, 3 mapped to repos, raw item bytes dropped), mention capture, HN deletion sync (CB-02). Migrations 0004–0005. 252 tests pass. ADR-031.
- 2026-09-25 M1-T18 detection re-plan (researcher): docs/research/detection-replan.md. Findings (pending verifier): the stargazers list API is closed for non-admin repos since 2026-06-30; the new stargazers/history endpoint gives daily net counts; GH Archive coverage about 2% on 2026-09-24; own /events polling is capped at about 3%; the OpenDigger mirror is near-complete but unlicensed. Resource use: the measurements ran on the owner's logged-in `gh` session (no project token yet): about 2,100 REST requests (about 1,000 wasted on 404s from a script bug), under 60 GraphQL points, 5 searches, all within limits; about 11 GB of OpenDigger files streamed; stargazer logins used only for counts and deleted. Future measurements must use the project GITHUB_TOKEN (H1) and a stated budget.
- 2026-09-25 verifier detection-replan @fbfcc88: FAIL (citations and arithmetic only). Technical claims 1–6, 8, 9 verified live on a small budget (15 REST, 3 GraphQL points, 3 searches): stargazers API closed, star-history endpoint works (days not UTC-aligned, consistent with Pacific time), /events cap, GraphQL batching cost, Search caps, ToS §H. Fixes needed: ClickHouse/PingCAP citations, Tier 1 page range 1–33, OpenDigger 1.013× is agreement not coverage, per_page=6, OpenDigger latency ≈ 1–1.25 h. → ADR-032.
- 2026-09-25 D1 preview (API, operator login, audit log, React UI; ADR-034) and scheduler (health, alerts, Docker/systemd; ADR-033) merged. Fixed: deletion-sync crashed without PSEUDONYM_KEY (now a clear usage error). 335 tests, UI lint/type/test/build pass.
- 2026-09-25 verifier M3-T6/T7/T8 (ADR-032/035/036 docs + schemas) @2521652: FAIL. Thresholds v0.2.0 = v0.1.0 on all 60 numbers; amendments OK (24/24 quotes match, originals untouched, DB has no outcome tables, 0 cases); scan clean. Fixes: star+fork consistency (decided: keep both), LIA/DPIA still called ADR-036 'proposed', source-matrix still prescribed the stargazers API, case schema bot_filter fields. Delegated.
- 2026-09-25 M1-T24 detection v1 (engineer): GitHub connector (one token, budgets, rate limits, ETag, poll interval), watch list + GraphQL count snapshots, search sweeps, HN screen, star-history client, detection v1, per-repo events (off; CB-22/23 built), six scheduler jobs (skip without a token). Migration 0007. 379 tests pass. Tested on a fake GitHub API only. First live steps when the token arrives: M7 seed (search-sweep --kind new --max-requests 60, hn-screen, watchlist-counts --max-points 50, budget --hours 1), M3 GraphQL scale, M2 DST check (2026-11-01), M1 coverage audit (200 repos), M4 14-day recall. ADR-037.
- 2026-09-25 verifier M3-T9 @66acb80: FAIL. Earlier items fixed; CB-22/23 code matches docs except the ceiling; thresholds v0.2.0 = frozen v0.1.0; prereg originals untouched. Fixed in fd7cdef/16be613: config made 16 the ceiling for GITHUB_EVENTS_RETENTION_DAYS (my own ADR-038 edit; now default 16, ceiling 30, test covers 17/30/31); spec §2.1 aligned with ADR-037; CB-24 registered in the DPIA; schema description names; .env.example; prereg naming erratum; env-independent CLI test.
- 2026-09-25 incident (second time): fd7cdef was pushed with a failing private-data scan (a dummy DSN with inline credentials in a test, no real secret), because the shell chain didn't stop on the scan failure. Fixed in 16be613; CI green. Rule from now on: run the scan and the commit as separate tool calls, and commit only after reading a clean scan result.
- 2026-09-25 verifier M3-T9 round 2 @80918ee: PASS. Retention default 16 / ceiling 30 (tested 7/17/30 accepted, 31 rejected); spec §2.1 aligned with ADR-037; CB-24 registered; non-blocking items done; retention values consistent everywhere; prereg originals and amendments unchanged; unit 293 pass, M1-T24 integration 14 pass; scan clean (208 files).
- 2026-09-25 verifier M4 method set @a6a9e3d: FAIL (one item). Pass: frozen files unchanged; DB has 0 cases, no coding outputs; codebook v0.2.0 G1 enums identical (79 values); split rule re-implemented, all test vectors reproduce; nothing relaxed vs PRD §9.2 / WO G1. Fail: my forecasting amendment 2 left H-sealed inside the A30 target cells and didn't quote old/new text; README listed it as an erratum. Fixed with forecasting amendment 3 and README rows.
- 2026-09-25 verifier M4 method set round 2 @9178f90: FAIL (one item). My amendment 3 cited 'original §2' (actually §1.1) and its A30 text dropped amendment 1's day-mapping clause. Fixed with forecasting amendment 4, whose quotes were extracted programmatically from the frozen files.
- 2026-09-25 capture follow-ups (engineer): CB-24, CB-23b, CB-18b, M1-T19 backfill, M1-T22 front-page minutes, M1-T23 (except live Algolia). Migration 0008. 410 tests pass. Found: name opt-outs stored as unkeyed SHA-256 (reversible for public repo names) → CB-13b must fix before production (ADR-040).
- 2026-09-25 verifier M4 method set round 3 @c73bf8c: PASS (M4-T1d, M4-T2b, M4-T2c, forecasting amendments). Amendment 4 quotes verified by script; freeze hashes confirmed; frozen files unchanged; DB has 0 cases, no outcome tables; the chain gives one current definition, consistent with the calibration pre-registration and the outcome spec.
- 2026-09-25 engineer batch: CB-13b keyed name opt-outs, M4-T4 split + sealed guard + settle-lag collection, M1-T20 JSONL export (+ S3 in CI), M1-T26 liveness, v1 detection hours in the case API. Migrations 0009–0011. 457 tests pass (DB + S3). Found: opt-out purge misses the M1-T24 tables → CB-13c. ADR-042.
- 2026-09-25 compliance runbooks: key rotation (CB-09), RoPA (CB-15), breach (CB-16), controller duties (CB-21). Finding: changing PSEUDONYM_KEY silently disables all opt-outs → CB-25 key-fingerprint check made a production precondition; no scheduled rotation until CB-26/27 (ADR-043). New items CB-25..33; LQ-30/31.
- 2026-09-25 CB-13c repo purge registry + schema test; CB-17 encrypted backups (age/gpg) with a restore that re-applies deletions (tested: no deleted data returns). 466 pass locally; backup tests run in CI with age + pg client 16 (verified in an Ubuntu 24.04 container). ADR-044.
- 2026-09-25 CB-25 key fingerprint, CB-29 secret hygiene, CB-32, CB-33, CB-34 (engineer). Migration 0012. 489 tests pass. Fixed a bug where an empty opt-out list skipped the key check. ADR-045.
- 2026-09-25 CI: gitleaks flagged synthetic test values in test_secret_hygiene (false positive); marked with gitleaks:allow; CI green.
- 2026-09-25 CB-26 rekey (atomic re-derivation), CB-27 not needed, CB-28, CB-30, CB-31, CB-17b doctor checks (engineer). Migration 0013. 526 tests pass. ADR-046.
- 2026-09-25 CB-35 (restore refuses pre-reset backups) and key-rotation runbook v0.3 with compliance status sync. 529 tests pass. Pausing new work here pending the owner's decision (asked whether to continue with smaller items).
- 2026-09-26 M11 cleanup: docs part (214986f) and code part (next commit). 214986f accidentally included staged deletions and CI failed; repaired by the code commit. 474 tests pass, UI and compose OK, scan clean. ADR-050, ADR-051.
- 2026-09-26 verifier M11 @3525032: FAIL. Tag, migration 0014, registries, 474 tests, CI (repairs 214986f), amendments and scan OK. Fixes: stale docstrings; UI 'Live now' velocity strip; codebook and outcome-model cite deleted code, the retired thresholds JSON and the dropped hourly cache; compliance docs describe removed processing; data-cache inventory missing; single-day spikes marked multi_peak → ADR-052.
- 2026-09-26 seed-project lookup for the owner's brief (private results in data/briefs): 25 GitHub API requests on the owner's gh session (cap 30), no stargazer lists. M11 fixes: docstrings, launch-mode strip replacing 'Live now', ADR-052 burst rule (d−1 ≤ last burst day) with tests, `report inventory` + docs/reports/data-inventory-2026-09-26.md (local cache essentially empty), codebook v0.3.1, outcome-model, compliance sync.
- 2026-09-26 verifier M11 round 2 @1c5bf6c: PASS. **M11 accepted.** All six fixes verified; the single-spike and two-peak burst cases reproduced; inventory reproduced from the DB; 479 tests, UI checks, CI and scan clean. Follow-ups (M5/M8 pointers, test docstring, project name in the public backlog) fixed in the next commit.
- 2026-09-26 M12 research brief v1 (engineer): schema + model, private versioned store, CLI incl. estimate and BudgetGuard, cache reuse and provenance (migration 0015), /briefs UI, example brief and 'Your first brief' guide. 568 tests, UI checks and scan pass. Found: seed-project names from the owner's brief in BACKLOG and ADR-054 → made generic (ADR-055.1). Owner confirmed training off.
- 2026-09-26 verifier M12 @9f3778d: FAIL. Suite 568 passed; estimate, hard stop (reproduced), cache reuse, scan (planted briefs blocked), API auth, schema drift and migration 0015 OK. Fixes: (1) R18.7 LLM expansion missing; (2) stale edit via `brief edit --from` without --base-version overwrites a newer version; (3) the owner's core field and widening steps were in ADR-054.2 and PRD R4.10 → made generic.
- 2026-09-26 M12 fixes: R18.7 expansion (proposal-only, minimal input), stale-edit defaults, brief schema v1.1 (reference cases, distribution examples, report options), scan catches more brief layouts, migration 0016; fixed a time-dependent test. 601 tests, UI and scan pass. ADR-058.
- 2026-09-26 verifier M12 round 2 @16c0b1b: PASS. **M12 accepted.** 601 passed; expansion reproduced (only description, target users, business model and include/exclude reach the model); stale edits refused on the CLI default path and the API; owner brief content absent from the tree; schema v1.1 and migration 0016 OK. Follow-ups: scan should block expansion proposal files; CLI edit files without a version should be refused; stale estimate-v0 / brief-v1 labels.
- 2026-09-26 Owner Directive 001 received (Documents/OWNER_DIRECTIVE_001.md). Awaiting owner answers on committing §4 brief content and on stargazer timestamps (unavailable since 2026-06-30) before applying it.
- 2026-09-26 verifier M20 round 1 @8fd91b7: FAIL (CR refs, backlog remap, reports vs §8.6, Wayback, ADR-022, agent files, env, milestones) → fixed in b2d74d7 (ADR-073). Round 2 @b2d74d7: FAIL on 4 one-line items (analyst/verifier pilot-only pre-registration, PRD binding-source line, LEGAL_REVIEW_H2 hold conditions) → fixed in the next commit. Operator guide's stale text is flagged for M27.
- 2026-09-26 verifier M20 round 3 @0d2ff13: PASS. **M20 accepted.** Follow-ups: operator guide (M27); pre-directive compliance templates and the example brief's subscription_share to be rewritten or bannered in the §9 template step; BACKLOG header wording.
- 2026-09-26 History rewrite (owner-approved, 0 forks, 2 stars): commits from 0330bd4 onward on main were rewritten to remove owner-brief content (not repeated here) and force-pushed (c24fe6d → 7a823ea). Earlier commits, frozen pre-registrations and the archive tag are unchanged; the final tree is identical. Old→new map: ops/history-rewrite-2026-09-26.md. A private pre-rewrite backup is kept outside the repo. A GitHub Support request to purge cached views of the old commits is prepared for the owner to submit (support requests need her GitHub login).
- 2026-09-26 M21 built (two engineers + a redaction follow-up): roles and buckets with handles purged (migrations 0017–0019), shortlist-only mentions, snapshot retention by report, per-stage models, Batch API, prompt caching, evidence trimming, per-call keyless aliases for model inputs, keyless logs, cost estimator and hard caps, cost ledger (0020), anomaly checks, briefs store moved to ~/.pigtail/briefs. 693 tests, UI and scan pass. Incident: the first M21b test run wrote synthetic test briefs into ~/.pigtail/briefs before the owner's brief was moved there; they were deleted, and tests now use an isolated temp dir. ADR-074.
- 2026-09-26 verifier M21 round 1 @40ee8a3: FAIL on 1 item (ADR-070.4 / R3.3: outcome model, codebook and literature notes still specify the account-level fake-star method and its filtered series, which need data removed in 0017). Everything else passed with reproduction (purge, bot flags, retention, shortlist scope, per-stage models, batch resume, caching, trimming, keyless aliases, cost caps and ledger, briefs isolation, full checks, CI). Non-blocking: M22 must always pass a per-item estimate to `before_submit`.
- 2026-09-26 M21 fix: outcome model v2.1, codebook v0.4.0, candidates v0.4.0 and literature §2.4 updated for the anomaly checks (ADR-075). Owner's email provider for the notice: Google Workspace (from public MX records; owner to confirm).
- 2026-09-26 verifier M21 round 2 @4d1b3b0: PASS. **M21 accepted.** Anomaly-check docs match the code exactly; citation quotes re-checked; CI green; scan clean. Three wording fixes applied.
- 2026-09-26 Backups set up on the owner's Mac: encrypted backups to an external drive folder (age), matching PostgreSQL 16 client installed via Homebrew (a v18 client broke the restore: `transaction_timeout`, rolled back safely), OPTOUT_KEY generated (it had never been set), first backup + test restore into a throwaway DB (counts matched), test copies removed. Fixed an unquoted value in the env files that broke `--env-file` loading.
- 2026-09-26 M22 built: `pigtail run --brief` (checkpoints, resume, approval gate), discovery (GitHub search and topics, Show HN project-level only, awesome lists), batched Haiku relevance filter, shortlist review (UI + CLI, logged decisions, precision, finalize), migration 0021. 719 tests, UI, compose and scan pass. Example-brief dry run: this stage ~USD 0.56; whole brief ~USD 17 with batching (planning model, upper bound). ADR-076.
- 2026-09-26 verifier M22 round 1 @7a58427: FAIL. Checks green (ruff, mypy, 719 passed/5 skipped with DB+S3, UI, compose, scan, CI). Crash/budget resume, approval gate, handle redaction reproduced OK. Fixes: named-project names stored in evidence.url (Show HN/GitHub name queries, ADR-076.6); batch fallback calls skip the budget check (cap exceeded in probe); Shortlist.add and unresolved matches ignore the refusal list (CB-13); list of brief fields filled with defaults missing (D7/R4.7). M22 remainder not built: outcome sort/selection/determinism, balance, sensitivity (R4.8/R4.9/R4.3), live example run.
- 2026-09-26 M22 round-1 fixes: name searches store `named:<panel>:<i>` in place of the query (evidence_url override); fallback calls get their own budget check at the standard price; refusal list enforced in Shortlist add/members/view and in discovery's candidate matches; `Brief.defaulted_fields()` shown as "Brief fields to confirm" (warnings shown separately); finalize runs in one transaction. 724 passed/5 skipped (DB+S3), UI 25 passed. Owner: brief cap lowered to USD 90 (brief v4); approved the expansion proposal and the first run up to the shortlist (H6).
- 2026-09-26 M22 remainder built (engineer, worktree): selection stage after a final shortlist: star-history fetch (checkpointed), anchors, outcome sort with percentiles and thresholds, winners and exactly matched losers, widening then ADR-053.2 fallback steps logged step by step, exemplar losers, balance (SMD, exact-match check, headline exclusion), sensitivity (primary swap, band shift, weights, anomaly-flagged exclusion); migration 0022 (brief_selection, brief_selection_case) in REPO_TABLES, export and inventory; `pigtail brief selection show`. Only star metrics have data; other dimensions are unknown (no connector). 746 tests pass (21 new), 5 skipped; ruff, mypy clean. ADR-077.
- 2026-09-26 owner brief v5 (private): discovery + relevance done (brun_89a…; 1,500 candidates, cap reached of 1,964 seen; Haiku batch $0.59, expansion $0.04). Owner confirmed the 5 distribution-exemplar repos (CLI, as owner), then asked to finalize without item review: relevant bulk-accepted, uncertain bulk-rejected, finalized (686 on the shortlist, 4 reference cases unresolved). The logged precision "100 % owner-checked" is **not a measurement** (no item review; R4.7): treat v5 precision as unmeasured. Follow-up M22-P: label bulk decisions on the filter's own verdict as "not item-reviewed" in the precision label.
- 2026-09-26 verifier M22 round 2 @851d58c: FAIL. Checks green (ruff, mypy, 746 passed/5 skipped DB+S3, UI 25, scan, CI). Fixed: named-project names, refusal list, defaulted fields. Selection meets the specs (determinism, exact match/SMD, fallback logs, exemplars, sensitivity, provenance, purge/export/inventory, no brief text). Required: fallback budget check uses stale spend (cap exceeded in probe, 0.30 > 0.25); no pre-registration gate before the outcome sort; adoption-primary example brief gives 0 winners with no milestone owning outcome connectors; headline exclusion (11-18/20 pairs) to settle before any outcome sort; live example-brief acceptance run not done.
- 2026-09-26 M22 round-2 fixes (builder, worktree; ADR-078): BudgetGuard re-reads the brief's ledger total before every money check (probe reproduced: 0.10 charged + 0.20 fallback > 0.25 now stops); pre-registration gate before the outcome sort (`pigtail brief preregister`, --print-hashes, TEMPLATE-brief.md, migration 0023 `brief_preregistration`; `pigtail run` exit 7, nothing fetched or stored); headline-first matching (selection-v2; headline exclusions on the seeded 80-case fixtures 133 → 120 of 200 pairs, range 8–18 → 3–17, within 0–2 pairs of the data-limited bound); missing language = mismatch; as_of folded into the data version; refusal list re-checked at finalize and before the selection fetch; bulk decisions logged (`bulk_id`), 'not item-reviewed' label (M22-P); shortlist show prints defaulted fields and warnings; Finalize says why it is disabled; sensitivity says why an alternative doesn't apply. 760 passed/5 skipped (DB+S3), UI 25 passed, ruff/mypy clean. Not built (owner decision): outcome connectors, live acceptance run.
- 2026-09-26 M22 follow-ups (builder, worktree; ADR-079, ADR-080): `pigtail brief shortlist carry-forward <id> [--from] [--to] --as --reason` copies a final shortlist to a later version that differs only in success.*, panel.*, report.*, notes or store metadata (generic allow-list from the model dumps; other changes refused with the paths): candidates with carried_from_version, each latest decision as a new row keeping the original role/via/reason/bulk marker/time plus the carry's role/reason/time, named-project confirmations, final scope; precision label + "carried from vN"; refused repos dropped and counted; a `carried_forward` brief_runs row linked to the source run, so the next run does only the selection behind the target's pre-registration (migration 0024; operator guide). Example brief ranks on attention (top_quartile, no minimums, steps [relax_primary_to_top_third]); WORK_ORDER v2.2 adds M23b outcome connectors; HUMAN_INPUTS note; `make_guard` gets the fresh ledger reader (CLI and web). Tests 769 passed / 5 skipped (DB+S3), +9. No network, no spend.
- 2026-09-26 M22 live acceptance run (example brief as shipped, attention primary; throwaway instance: own DB `pigtail_accept`, local snapshots, own briefs dir and opt-out key; real GitHub token, api backend; owner approved ~$0.60). `--dry-run` estimate shown; approved run killed 5 s after the relevance batch was submitted, resumed: same run id, batch collected, not resubmitted (one extra batch for missing verdicts). 623 candidates (114 relevant, 37 uncertain, 470 not relevant); every candidate has a source, every verdict a reason. Decisions: 4 single CLI (accept/reject/add with reasons), 2 via the web API on a throwaway :8081 instance (precision 60 %, below target, flagged), then bulk; finalized 114 (label "109 of 114 by bulk action, not item-reviewed"). Selection refused without pre-registration (exit 7, nothing fetched); after a throwaway pre-registration it ran with live star history (114 repos): 20 anchored (Show HN launch or burst), 94 `no_anchor`; attention observed for 19, pending 1 → below the minimum population (20) → no percentiles, **0 winners**; steps, balance (n/a) and sensitivity (not-applicable reasons) reported. Recompute from stored data: same result and inputs hash. pg_dump: no brief text, no e-mails, no handle keys. LLM spend $0.23 (Haiku batch). Fixes: `pigtail run` prints "refused" (not the run status) when the pre-registration gate stops it; the Anthropic SDK's `httpx2` logger silenced like httpx.
- 2026-09-26 verifier M22 round 3 @3c94033: FAIL. Checks green (ruff, mypy, 769 passed/5 skipped DB+S3, UI 25, scan, CI); round-2 fixes 1, 2, 4, ADR-079 and ADR-080 reproduced OK. Required: launch anchors use only Show HN posts discovery recorded (§2.1; acceptance run: discovery found 13 of 19 in-window launches, 5 no-anchor repos had one → 24 observed ≥ 20), same-day launch loses to a day-precision burst, anchor rule not in the pre-registered hash; re-run the acceptance selection live. Owner's outcome sort on hold until then, plus a pre-registration amendment (new hashes, H1 anchor confound). Correction to the acceptance-run entry above: the discovery checkpoint and one relevance reason in the DB hold brief keyword/exclude text (private DB, allowed by ADR-076; "no brief text" referred to named projects and free-text fields).
- 2026-09-26 M22 acceptance selection re-run live after round-3 fixes (throwaway instance; example v1 → v2 notes-only, carried forward, pre-registered v2; HN lookups live, no LLM spend): selection-v3, 33 of 114 anchored (was 20), 9 winners (after widening to distance 2 and relaxing to top third), 1 matched loser (18 in the pool; winners' launch-signal SMD 4.04 before matching, so few losers within the caliper), 0 headline pairs (the one pair excluded on language and age), sensitivity: Jaccard min 0.64 / mean 0.71, 3 definition-sensitive. Recompute from stored data: same result and inputs hash. No HN raw page kept. Brief 1 v6: amendment 1 written (new selection hash, H1 restricted to launch-anchored pairs), to be recorded before its outcome sort.
- 2026-09-26 verifier M22 round 4 @7d8855f: FAIL. Checks green (ruff, mypy, 793 passed/5 skipped DB+S3, UI 25, scan, CI). Day-precision anchor rule, anchor-v2 in the hash, lookup storage (no author/title, raw dropped) and amendment-1 hashes reproduced OK; 1 matched loser is the LSM caliper under attention primary (15/18 pool losers outside it), not a pool bug. Required: title matching not conservative (acceptance run: at least 7 of 10 title anchors are other posts, e.g. "Visual Studio Code"→studio, "Launch HN: Poly", "just TOML"; can steal a true burst anchor), selection must refuse when the launch lookup is skipped (HN off by default), then anchor-v3/selection-v4, brief-1 amendment 2 and a live acceptance re-run. tags=launch_hn exists (ADR-081 says not). Owner's outcome sort on hold. Correction to the re-run entry above: widening to distance 2 was not applied (added 0).
- 2026-09-26 M22 acceptance selection re-run live under anchor-v3 / selection-v4 (throwaway instance; example v2 → v3 notes-only, carried forward, pre-registered; 342 HN lookups, no LLM spend): 26 of 114 anchored; 142 title-only lookup hits rejected (not_product_slot 120, common_word 13, links_other_repo 7, short_name 2); 8 winners (widened to distance 1, relaxed to top third), **0 matched losers** (17 in the pool; launch-signal SMD 4.30 before matching: no pool loser lies within the 0.5 SD LSM caliper of any winner); sensitivity Jaccard min 0.62 / mean 0.68. Under attention primary the LSM caliper leaves no matchable losers in this field: an owner decision (before brief 1's outcome sort, by ADR) whether to keep it.
